<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mmpud.project.daycountwidget"
    android:versionCode="4"
    android:versionName="1.3" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/widget_preview"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <receiver
            android:name="mmpud.project.daycountwidget.DayCountWidget"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.appwidget.action.WIDGET_UPDATE_MIDNIGHT" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/day_count_widget" />
        </receiver>

        <service android:name="mmpud.project.daycountwidget.DayCountWidget$UpdateService" />

        <activity
            android:name="mmpud.project.daycountwidget.DayCountConfigure"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <activity
            android:name="mmpud.project.daycountwidget.DayCountDetailDialog"
            android:excludeFromRecents="true"
            android:label="@string/title_activity_day_count_detail_dialog"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" >
        </activity>
    </application>

</manifest>